# Project Rules

- **Git Commits & Pushes**: Do NOT stage, commit, or push any changes using Git unless the user has explicitly requested you to do so in the active prompt. Always verify files, run builds, and check status, but do NOT execute git commit/push operations autonomously.
